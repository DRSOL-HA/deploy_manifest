apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: webboard
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:DRSOL-HA/deploy_manifest.git
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: board
  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

